{% extends 'base.html.twig' %}

{% block body %}
    <div class="row mt-5">
        <div class="col-3">

        </div>
        <div class="col-6">
            <div class="row">
                <div class="col-2 mr-5">
                    <img src="{{user.avatar}}">
                </div>
                <div class="col-5">
                    <p class="font-weight-bold">email: {{user.email}}</p>
                    <p class="font-weight-bold">date adhésion: {{user.joinDate | date}}</p>
                </div>
            </div>
        </div>
    </div>
    <div style="text-align: center;">
        <button class="btn btn-success">
            <a class="text-white" href="#">
                Envoyer un mail
            </a>
        </button>

    </div>

{% endblock %}