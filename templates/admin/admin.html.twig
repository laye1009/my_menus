{% extends 'base.html.twig' %}
{% block stylesheets %}
    <style>
        tr:nth-child(even) { 
        background-color: Lightgrey; 
        } 
        thead tr th{
            text-align:center;
        }
        td {
            text-align: center;
            width: 20%;
        }
        table {
            margin-top: 10%;
        }
        thead{
            font-weight: bold;
            text-decoration: underline;
            background-color: lightgrey;
        }
    </style>
{% endblock %}

{% block title %}Hello AdminController!{% endblock %}

{% block body %}
    <div class="container">
        <table>
            <thead>
                <tr>
                    <th>Nom</th>
                    <th>Description</th>
                    <th>Prix</th>
                    <th>Image</th>
                    <th>Catégorie</th>
                    <th class="ml-10">Action</th>
                </tr>
            </thead>
            <tbody>
                {% for item in items %}
                    <tr>
                        <td>{{ item.name }}</td>
                        <td>{{ item.description }}</td>
                        <td>{{ item.price }}</td>
                        <td>{{ item.image }}</td>
                        <td>{{ item.category}}</td>
                        <td>
                            <span>
                                <a href="{{path('admin_edit',{'id':item.id})}}">
                                    <img style="width:20px;height:20px;" src="{{asset('images/autres/pencil.png')}}">
                                </a>
                            </span>
                            <span>
                                <a href="{{path('admin_delete',{'id':item.id})}}">
                                    <img style="width:20px;height:20px;" src="{{asset('images/autres/trash.png')}}"></a>
                                </a>
                            </span>
                        </td>
                    </tr>
                {% endfor %}
                
            </tbody>
        </table>
        {{ knp_pagination_render(items) }}
        <div class="row">
            <div class="col-3"></div>
            <button class="btn btn-success col-6">
                <a href="{{path('item_create')}}" class="text-white bg-dark">Ajouter un nouveau produit</a>
            </button>
        </div>
    </div>
{% endblock %}
